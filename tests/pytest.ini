[pytest]
# Run from inside tests/ so conftest.py (which wires sys.path for the scraper and
# pipeline packages) is picked up as the rootdir conftest.
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -ra
