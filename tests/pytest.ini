[pytest]
# Run from inside tests/ so both test_*.py here and tests/integration/ are
# discovered. tests/integration/conftest.py puts the repo root on sys.path
# itself, so no root-level conftest.py is needed.
testpaths = .
python_files = test_*.py
python_functions = test_*
addopts = -ra
