# Test dependencies for the Python side of the CA ADU Zoning API.
#
#   pip install -r tests/requirements-dev.txt
#
# test_pipeline.py needs only pytest (baselines.py / validate.py are pure stdlib).
# test_scraper.py additionally needs the scraper runtime deps below; if any are
# absent it skips itself cleanly via pytest.importorskip rather than erroring.

pytest>=8.0

# Scraper runtime deps (mirrors scraper/requirements.txt) - imported by the
# adapters and db module under test. No browser download is required: Playwright
# is only imported, never launched, and nothing hits the network.
playwright==1.61.0
beautifulsoup4==4.13.5
lxml==5.3.2
supabase==2.15.3
httpx>=0.27,<0.29
tenacity==9.1.2
