# Test dependencies for the Python side of the ADU Atlas API.
#
#   pip install -r tests/requirements-dev.txt
#
# tests/integration additionally needs psycopg (v3, from services/requirements.txt)
# to talk to the ephemeral PostGIS container it spins up; it skips itself
# cleanly (not a failure) when psycopg, psql, or the database are unavailable.

pytest>=8.0
psycopg[binary]>=3.1
