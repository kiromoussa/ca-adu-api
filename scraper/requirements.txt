# CA ADU Zoning API - scraper worker dependencies
# Pinned to versions current as of 2026-07. Install browsers separately:
#   playwright install --with-deps chromium

playwright==1.61.0
# curl_cffi impersonates a real Chrome TLS/JA3 fingerprint, which gets past the
# Cloudflare bot check on American Legal (codelibrary.amlegal.com) that blocks
# both plain HTTP clients and headless Playwright. Used by the ALP adapter.
curl_cffi>=0.7,<0.11
beautifulsoup4==4.13.5
lxml==5.3.2
supabase==2.15.3
httpx>=0.27,<0.29
tenacity==9.1.2
python-dotenv==1.1.1
