# CA ADU Zoning API - extraction + validation pipeline (Render)
# Python 3.11+

# Supabase (service-role writes to adu_rules / reads from zoning_sections)
supabase>=2.9.0

# LLM extraction providers (pipeline uses whichever env is configured)
anthropic>=0.69.0          # Anthropic path: output_config structured outputs
openai>=1.55.0             # Azure OpenAI path: json_schema strict outputs

# Schema validation + typing
jsonschema>=4.21.0
pydantic>=2.6.0
